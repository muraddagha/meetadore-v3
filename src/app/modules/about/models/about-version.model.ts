export interface IVersionModel {
  updateDate: string;
  createUserId: string;
  versions: string;
  releaseDate: string;
  updateUserId: string;
  active: string;
  id: string;
  createDate: string;
}
