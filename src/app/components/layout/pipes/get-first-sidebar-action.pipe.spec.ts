import { GetFirstSidebarActionPipe } from './get-first-sidebar-action.pipe';

describe('GetFirstSidebarActionPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFirstSidebarActionPipe();
    expect(pipe).toBeTruthy();
  });
});
